## 1. Shared loading primitives

- [x] 1.1 Добавить `src/shared/ui/skeleton/` с типизированным базовым `Skeleton`, декоративной семантикой и CSS-анимацией, отключаемой через reduced motion.
- [x] 1.2 Добавить и экспортировать skeleton-композицию для закрытого состояния `CardAmount`, сохранив его внешние классы и размеры.

## 2. Compact amount widgets

- [x] 2.1 Перевести `YearSavings`, `YearIncomes` и `YearExpenses` на `CardAmount` skeleton без схлопывания годовой колонки.
- [x] 2.2 Перевести `Balance`, `Incomes` и `Savings` на `CardAmount` skeleton, сохраняя контент при refetch и учитывая все обязательные запросы каждого виджета.
- [x] 2.3 Добавить стабильные loading и empty состояния для `LastYearRest`, не удаляя его слот из раскладки после пустого успешного ответа.

## 3. Chart and progress widgets

- [x] 3.1 Добавить skeleton-состояния с целевыми размерами областей графика в `ExpensesByMonths`, `ExpensesByAnnualCategories` и `ExpensesByDays`.
- [x] 3.2 Добавить структурные строки-заглушки и общую минимальную высоту loading/content состояний в `ExpensesByCategories` и `ExpenseLimits`.
- [x] 3.3 Заменить возврат `null` после пустых успешных ответов в условных chart/progress виджетах на стабильные карточки с сообщением об отсутствии данных.

## 4. Table widgets

- [x] 4.1 Добавить skeleton карточки и строк таблицы в `Expenses`, сохранив существующие ширину и внешнюю оболочку.
- [x] 4.2 Добавить skeleton карточки и строк таблицы в `Debts`, сохранив заголовок и геометрию controls.
- [x] 4.3 Заменить текстовое loading-состояние `Plans` на строки skeleton внутри существующей карточки и согласовать минимальную высоту с empty/content состояниями.

## 5. Query state and accessibility audit

- [x] 5.1 Проверить loading-условия всех затронутых виджетов: skeleton должен появляться только при отсутствии обязательных данных, а доступный контент должен оставаться во время фонового refetch.
- [x] 5.2 Добавить каждому loading-контейнеру `aria-busy`, одну скрытую текстовую индикацию загрузки и исключить декоративные skeleton-блоки из accessibility tree.
- [x] 5.3 Убедиться, что в `src/widgets/` не осталось видимых текстовых заглушек «Загрузка...» и неявных `null`-состояний, нарушающих стабильную раскладку.

## 6. Validation

- [x] 6.1 Запустить `npm run lint` и устранить ошибки в затронутых файлах.
- [x] 6.2 Запустить `npm run build` и проверить успешную TypeScript/Vite сборку.
- [x] 6.3 Запустить приложение и вручную проверить `/main` при замедленной сети: первичную загрузку, переключение периода без кэша, фоновые refetch и пустые ответы.
- [x] 6.4 Проверить светлую и тёмную темы, а также режим `prefers-reduced-motion: reduce`, убедившись в сохранении читаемости и размеров виджетов.
